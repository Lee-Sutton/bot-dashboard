{
  "name": "bot-dashboard",
  "private": true,
  "scripts": {
    "cypress": "node_modules/.bin/cypress open",
    "start": "meteor run",
    "test": "meteor test --driver-package meteortesting:mocha --port 4000"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0-beta.42",
    "meteor-node-stubs": "^0.3.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "cypress": "^2.1.0"
  }
}
